import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class SpaceH32 extends StatelessWidget {
  const SpaceH32({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 32.h,
    );
  }
}

class SpaceH48 extends StatelessWidget {
  const SpaceH48({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 48.h,
    );
  }
}

class SpaceH64 extends StatelessWidget {
  const SpaceH64({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 64.h,
    );
  }
}

class SpaceH128 extends StatelessWidget {
  const SpaceH128({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 128.h,
    );
  }
}

class SpaceW32 extends StatelessWidget {
  const SpaceW32({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 32.w,
    );
  }
}

class SpaceW16 extends StatelessWidget {
  const SpaceW16({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      width: 16.w,
    );
  }
}

